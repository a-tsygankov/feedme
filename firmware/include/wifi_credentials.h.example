// Copy this file to wifi_credentials.h (which is gitignored) and fill
// in your home Wi-Fi details. main.cpp tries to #include this header
// if it exists; if not, Wi-Fi/NTP just don't run and the device falls
// back to monotonic-millis time as before.
//
// Phase 2.4 of docs/roadmap.md will replace this build-time mechanism
// with a captive-portal first-time setup flow that stores credentials
// in NVS at runtime.
#pragma once

#define WIFI_SSID  "your-ssid-here"
#define WIFI_PASS  "your-password-here"
